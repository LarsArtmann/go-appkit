// Package appkit provides a production-ready HTTP service framework that composes
// httputil (middleware, health), charmbracelet/log (structured logging), and
// go-error-family (error classification) into a unified service lifecycle.
package appkit
