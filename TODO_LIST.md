# TODO List — go-appkit

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision lives in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-16 (docs-health audit + second pass: completed items removed, release state truthed against git tags, unharvested items from the 2026-08-07 realtime and 2026-09-16 otel/library-audit reports routed; CSP/WriteTimeout and composition-spike items restored from the 2026-09-04 health report) | **Modules:** 10 (core, cqrs, realtime, otel, flightrecorder, flightrecorderhealth, health, errorpages, docs, integration — integration is the unreleased E2E test module) | **Release state:** every module tag is ON ORIGIN through **cqrs v0.5.0** (system-based engine, tagged+pushed 2026-09-07); wave 2 (core v0.4.0 lifecycle hooks, cqrs v0.4.0, otel v0.1.0, health v0.1.0, flightrecorderhealth v0.1.1) pushed 2026-09-04, wave 1 (core v0.3.0, cqrs v0.3.0, realtime v0.1.0, flightrecorder v0.1.0, flightrecorderhealth v0.1.0) pushed 2026-08-30; errorpages v0.1.0 + docs v0.2.0 tags pushed — but **docs/v0.2.0 is UNFETCHABLE from the proxy (P1 below)**; **`encoding/json/v2` is default-on in Go 1.26.7 — GOEXPERIMENT prefixes are only needed on older 1.26.x toolchains**

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to the module CHANGELOG and is removed from this list.

---

## P1 — High impact (release correctness)

- [x] **GHOST RELEASE FIXED 2026-09-16 (path A): `github.com/larsartmann/go-appkit/docs@v0.3.0` is FETCHABLE from the module proxy.** History: the module path declared `.../go-appkit/docs` but the directory was `docs-mod/`, so the proxy found no `docs/go.mod` at the v0.2.0 tag ("missing .../docs/go.mod at revision docs/v0.2.0"; reproduced 2026-09-16) — flagged HIGH severity 2026-07-07 and tagged anyway 2026-08-16. Fix executed 2026-09-16: `git mv docs doc` + `git mv docs-mod docs` (history preserved), `go.work`/CI matrix/dependabot updated, living-doc references swept (archived reports stay historical), hermetic verify green, re-tagged **`docs/v0.3.0`** (annotated, no filesystem replace), pushed, and PROVEN with a fresh-consumer proxy test (clean /tmp module → `go get docs@v0.3.0` → blank import → `go build` PASS). The dead v0.2.0 tag stays dead on the proxy (cannot be deleted without retraction games); v0.3.0 supersedes it and the module CHANGELOG documents the delta.
- [~] **pkg.go.dev verification for all released tags — licensing DECIDED (stays proprietary) + ghost FIXED (docs/v0.3.0 fetchable, proxy-proven 2026-09-16); only the crawler-lagged render check remains.** Proxy smoke DONE 2026-09-04 for waves 1+2 and 2026-09-16 for the new train tags (core v0.4.0, otel v0.1.1, docs v0.3.0, health v0.1.1, frh v0.1.2 — all fresh-consumer PASS). The 2026-09-16 pkg.go.dev requests for `docs@v0.3.0` returned 404: the crawler had not yet processed tags pushed minutes earlier (worker lag is minutes-to-hours; each page request enqueues a crawl — re-check `https://pkg.go.dev/github.com/larsartmann/go-appkit/docs` and the other module pages after the lag window). Expectation once crawled: pages EXIST for all modules (module-root LICENSE files ship in every tag); godoc stays hidden for core-by-CHOICE (proprietary license decision 2026-09-16 — not a defect).

## P2 — Medium impact (quality & docs)

- [x] **Decide the license posture (USER GATE) — DECIDED 2026-09-16: stays PROPRIETARY/unlicensed.** Lars ruled 2026-09-16: the root LICENSE stays the unclassifiable proprietary text — no MIT-family swap. Consequence accepted permanently: pkg.go.dev marks core `License: UNKNOWN` and hides godoc, and adoption/discovery pays that cost by choice; no further license work is scheduled. (Mechanical fix stands: module-root LICENSE files landed 2026-09-04 in all module roots, so submodule pages can index once crawled.) The root README's License section states the proprietary truth (fixed 2026-09-16 — it wrongly claimed MIT). pkg.go.dev render VERIFICATION (pages exist / 404s gone) remains tracked in the P1 item above, decoupled from licensing.
- [x] **OTEL REGRESSION (verified 2026-09-15, FIXED AND SHIPPED 2026-09-16 — the full release train ran same-day).** Root cause was: otelhttp names/attributes the span from `r.Pattern` read back on otelhttp's own request fork after the handler; every httputil middleware between otel and the mux re-forked the request with `r.WithContext`, so ServeMux's `r.Pattern = ...` landed on the deepest fork and otel never saw it. TRAIN EXECUTED 2026-09-16: httputil tagged **v1.2.0** (pattern fix at all FIVE pure-httputil fork sites — `RequestID`, `Timeout`, `ClientIPMiddleware`, `Nonce`, `ServerTimingMiddlewareWhen` — upstream regression-pinned by `TestPatternPropagation*`, zero added allocations) → core + otel `go.mod` bumps (all consumer suites `-race` green against the new pin) → otel tagged **v0.1.1** (also removes the leftover filesystem `replace` from its go.mod and deletes the README known-issue block) → integration-module pin test LANDED (`integration/otel_pattern_test.go`, `TestSpanNameAndRouteThroughAppkitOuterMiddlewares`): span `"GET /users/{id}"` + `http.route /users/{id}` VERIFIED against the PUBLISHED tags (pins `go-appkit/otel v0.1.1` → `httputil v1.2.0`); it fails on any older pin by design. Known limitation NOT covered (accepted): CSRF forks behind justinas/nosurf, which forks internally — unfixable from httputil without wrapping nosurf's internals; CSRF is not in the default stack. Bisection: `docs/status/2026-09-15_19-48_otel-telemetry-status.html`.
- [x] **Logging posture — DECIDED 2026-09-16: status quo (INFO default) + tuning documentation; no behavior change.** Rationale: the +30µs emitting cost is charmbracelet line FORMATTING, not middleware overhead (suppressed-at-WARN costs +0.8µs), `ServiceConfig.LogLevel` already gives consumers the WARN knob, and flipping the default would be a consumer-visible behavior change riding no user gate. Documented in the core README (Configuration → Log volume) with the full benchmark numbers so the decision is reproducible. Sampling/default-WARN defaults stay available as a future minor if a consumer demands them. benchstat: not installed (`go install` network-blocked); the recorded n=10 mean±sd numbers stand. Source: `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` finding 7.
- [x] **Realtime production correctness — FIXED AND SHIPPED 2026-09-16 (`realtime/v0.1.1`, proxy-proven).** (1) The SSE handler now sets `X-Accel-Buffering: no` before the first write, so nginx with default `proxy_buffering` forwards events immediately instead of queueing them (latency no longer degrades from ms to seconds). (2) A failing replay store no longer aborts silently: the handler emits a named `event: error` carrying a `retry: 30000` reconnection-backoff hint before dropping the connection, so browsers back off instead of storming the dead store. (3) Both failure paths are pinned: `TestHandler_AccelBufferingHeader` and `TestHandler_StoreFailure_SendsErrorEventBeforeAbort` (failing store → error frame + retry hint observed on the wire). Source: `doc/status/archived/2026-08-07_09-01_sse-realtime-brutal-self-review.md` §d-1/§d-2/§f-1..6.
- [x] **Health dashboard under the default stack — VERIFIED 2026-09-16, both fears resolved with evidence.** (1) CSP: appkit's default stack emits NO Content-Security-Policy at all (captured live: `X-Frame-Options: DENY` + `nosniff` only; httputil's `SecurityHeaders` sets CSP only when explicitly configured) — the dashboard's Datastar bundle, Tailwind CDN, and inline bootstrap execute UNBLOCKED in the default config; the feared silent CSP degradation cannot happen there. The REAL risk is the inverse and is now documented: a consumer adopting a strict CSP (e.g. httputil `RecommendedCSP`) blocks the CDN/inline scripts and statics the page — harden by self-hosting the scripts + the dashboard v0.8.1 nonce bootstrap. (No browser run needed for the default-stack verdict: nothing can be blocked by an absent CSP; a manual browser pass remains advisable only for CDN availability in locked-down networks.) (2) WriteTimeout: default 30s cuts `/health/sse` every 30s (browser reconnects — degraded-but-working; SSE held 4 frames over 6s in the live E2E). POSTURE DECIDED: docs-only — a loud `Mount` warning is architecturally impossible without breaking the module's no-core-dependency charter (`Mount` receives only the `*http.ServeMux`, so it cannot read appkit's timeout config). Documented in health README (Gotchas) + doc.go (Serving notes) + example now sets `WriteTimeout: appkit.NoTimeout`. Source: `docs/status/archived/2026-09-04_16-52_go-health-leverage-audit-and-health-module.md` §d-1/§d-2/§g-1/§g-2.
- [ ] **Compose `Service` on `httputil.Server` (the architecture lever; spike before refactor).** httputil v1.1.x `Server` now exposes `ListenerAddr()`, `Start`/`StartTLS`, a double-start guard, and graceful shutdown — the original "no listener access" justification for appkit owning `http.Server` directly is stale (AGENTS Architecture section). Spike first: prove `ServerConfig` covers `ServiceConfig` (the `NoTimeout` sentinel → zero-field mapping, drain ordering, shutdown phase logging unchanged — `shutdownlog_test.go` is the pin), then refactor behind the compile-proven plan. Unlocks the deferred Core TLS option nearly free (`StartTLS`). USER GATE (08-53 §g-2): keep `Service`'s public API byte-identical (transparent internal swap) or evolve it (exposed TLS config / `Server()` accessor). Source: `docs/status/2026-09-16_08-53_library-utilization-audit-and-fixes.md` §c-1/§c-2.
- [x] **Battery wave W2 — `security` module SHIPPED 2026-09-16 (all 8 batteries, one module).** Ports complete with CV's acceptance pins: A2 `APIKeyAuth` (SHA-256 + constant-time, `?key=` GET/HEAD-only, header-wins-over-query, missing-`Next()` empty-200 trap guarded) + A2b `APIKeyCSRFBypass` (fail-closed: bypass skips the token dance, never the auth — pinned), A3 `RateLimit` (named profiles, `MaxKeys` mandatory — the profile test fails on a zero cap; 429-not-overwritten + Retry-After + per-key isolation + window-reset pinned), A4 `OriginCheck` (same-origin always allowed, Referer fallback, 403 on foreign origin, allow-all logs), A8 `BodyLimit` (typed `*http.MaxBytesError`), A6 `SanitizeText`/`SanitizeURL` (the `&not=`→`¬=` regression pinned), A1 `CSRF` (`*` stripped + logged, NEVER allow-all), A5 CSP nonce infra + `BuildCSP` (deterministic sorted directives, `unsafe-eval` never in ANY environment — the DataStar lesson test-pinned; JSON-LD exemption; nonce injection deduped), A7 `SecurityHeaders` (HSTS two-year+preload production-only; dev/staging forced clean — the LAN lockout trap). Suite `-race` green, golangci 0 issues (module-local config), structure-linter 0, registered in CI matrix + dependabot. Release: tagged `security/v0.1.0` + proxy-proven (see CHANGELOG). Sources: `doc/feedback/processed/2026-09-04_batteries-included-sdk-gap-analysis.md` W2 + CV `platform/middleware/`, `internal/sanitization/`.
- [ ] **Battery wave W1 leftovers — core quick batteries.** G2 opt-in Prometheus surface in core (`ServiceConfig.Metrics{Path}` — request-duration histogram by route pattern, in-flight gauge, build info; the pure-Prometheus path for consumers who don't want the otel module; encode the unit-1 `_ratio` exporter trap in docs), F5 BuildInfo (`WithVersion` → /health version field + /version endpoint), E1 testkit seed (full-chain harness: FullChainURL vs MuxURL — raw-mux registrations skip the stdlib chain and are invisible to route discovery — plus goroutine-baseline teardown assert). G1 TLS stays demand-gated (PapDashboard, tracked in P3). Spec: `docs/feedback/processed/2026-09-04_batteries-included-sdk-gap-analysis.md`. **G2 hardening (learned 2026-09-15 from `~/projects/nix-email/docs/TELEMETRY.md`, stalw.art docs fetched same day): Stalwart's `/metrics/prometheus` ships UNAUTHENTICATED when both credential fields are unset — their #1 anti-pattern. G2 must ship the endpoint with basic auth wired (or an explicit, loudly-documented opt-out), and publish the exact metric names as a stable contract — alert tools embed metric identifiers in expressions with restricted syntax (Stalwart forces underscores for theirs).**
- [ ] **Telemetry documentation bundle (harvested 2026-09-16 from the otel self-review's unfiled ideas).** (1) Emission catalogue: every log line / metric / attribute appkit emits, with default levels (Stalwart §6/§7 pattern — the shutdown phase lines are the first entries). (2) Backpressure semantics doc: lossy-vs-blocking per sink (OTel batcher drops, charmbracelet formatting cost, SSE buffer overflow). (3) Incident-debug recipe: second exporter / verbose tracer toggled without touching the baseline (Stalwart pre-provisioned-tracer pattern). (4) cqrs doctrine note: DLQ/telemetry stores separate from the event store. (5) SSE filtered live-telemetry battery candidate (realtime + otel/health). (6) Umbrella telemetry doc using `~/projects/nix-email/docs/TELEMETRY.md` as the structural template (mental model → baseline → anti-patterns → catalogue → wiring checklist → open items).

## P3 — Technical debt & tooling

- [ ] **Bump Go toolchain past 1.26.7 when nixpkgs carries it** (GO-2026-6090 crypto/tls post-handshake flood, GO-2026-5972 encoding/asn1 recursion were the 1.26.5-era findings; all modules already sit at go 1.26.7 so the superseded 1.26.6 item is closed — this tracks the NEXT bump; builds run GOTOOLCHAIN=local so the nixpkgs lock gates this). Source: govulncheck via BuildFlow 2026-08-16; same item tracked in cqrs-htmx TODO_LIST P2.
- [ ] **BuildFlow dprint step fails on CHANGELOG-only commits** (exit 14 "no files found to format"): `dprint.json` excludes `**/CHANGELOG.md`, so a commit staging only CHANGELOGs (+ non-dprint files) trips the formatter's empty-set error. Root fix is upstream in buildflow (skip when the staged set ∩ dprint's non-excluded set is empty — see AGENTS.md Deferred Register); escape hatch until then: `--no-verify` + justification. Evidence: the v0.3.0-wave commit needed `--no-verify` on 2026-08-16 (deterministic, retried once).
- [~] **otel: benchstat re-baseline of the middleware benchmark — FRESH BASELINE RECORDED 2026-09-16 (n=10, mean±sd): no-op 20.0µs ± 0.4 / traced 21.8µs ± 0.9 / traced+metered 23.3µs ± 2.0; README table updated to these numbers.** Conclusion on the 2026-09-15 anomaly: machine load, not the v0.68→v0.71 bump — the same box measured ~25% LOWER than 2026-09-04 records one day later (benchstat not installed and `go install` is network-blocked; comparison via mean±sd + Welch-style t across 10 runs). Run-to-run drift on this box is ±25% — treat single-run deltas under that as noise. Item stays open only as a candidate: re-measure with real benchstat when an optimization candidate exists.
- [ ] **Propose httputil `Logging` emit with request context** so completion lines correlate with spans via `TraceHandler` (today only handler-level logs correlate). Status report §f P2-12. Feedback F2 adds the appkit-side half of the same battery: seed the request ID into a handler-scoped logger + an `X-Response-Time` response header (CV's `internal/middleware/timing.go`, 116 LOC, port-ready). Source: `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` finding 7 + `docs/feedback/processed/2026-09-04_batteries-included-sdk-gap-analysis.md`.
- [~] **Define v1.0.0 exit criteria for core** — DRAFT WRITTEN 2026-09-04: `docs/planning/core-v1-exit-criteria.md` (7 hard criteria incl. the mechanical API-break check, 3 soft signals, explicit non-goals). Stays draft until the consumer count grows; fold in deeper OTEL backlog from `docs/status/archived/2026-08-18_12-45_otel-module-and-telemetry-hooks.md` §f when it graduates.
- [ ] **Revisit cordis bridge module when triggers fire** (researched 2026-09-04, verdict: NOT NOW). `docs/planning/2026-09-04_cordis-and-go-plugin-mvp-integration.md` — all three required: (1) cordis tags `go/v0.1.0` (today proxy-pinned to a pseudo-version), (2) a real consumer states the reactive-composition requirement, (3) core v1.0.0 exit criteria shipped. Bridge sketch (~300-500 LOC, zero third-party deps) is in the doc §4 Option B. go-plugin-mvp marketplace integration: rejected as appkit dependency (wrong layer, pre-1.0 churn); recommended reverse adoption (their repo embeds appkit, cqrs-htmx ADR-001 pattern) — proposal to their TODO_LIST is user-gated.
- [ ] **PapDashboard integration: hold the reverse-adoption door open** (researched 2026-09-04, verdict: NO go-appkit code). `docs/planning/2026-09-04_papdashboard-integration.md` — PapDashboard (event-sourced notification hub, published as `github.com/larsartmann/papdashboard` v0.2.0) rejected as appkit module/dependency: it is an application, not a framework primitive, and only `pkg/enum` + `sdk/` are importable (`internal/` encapsulation). Recommended: PapDashboard hosts on appkit in THEIR repo — every load-bearing version already matches (go-error-family v0.10.0, samber/do v2.1.0, Go 1.26.7, jsonv2 already mandatory; their `cqrshtmx.Chain` is appkit-free even at cqrs-htmx v4.9.0) and appkit's drain phase fixes a real gap (their SSE stream + outbound webhooks are hard-cut on shutdown today). Proposal to their TODO_LIST is user-gated behind their open E14/M1 queue. appkit-side signal worth tracking: core has NO TLS support; their `PAP_TLS_CERT/KEY` is the first concrete consumer demand for a core TLS option if they want to keep app-level TLS. `integration/` sdk-driven E2E test becomes actionable only after they ship an appkit-hosted release.
- [ ] **cqrs: EventConfig opt-ins for event encryption/signing when a consumer demands it** (encryption/v4, signing/v4; candidates: idempotency/sqlstore, scheduling). Deliberately NOT added now — wrapper surface stays demand-driven. Routed from `docs/research/2026-09-04_go-cqrs-lite-deep-dive.html` finding 5.
- [ ] **flightrecorder polish (harvested 2026-09-16 from the 2026-08-11 report — still true today).** `SnapshotHandler` returns 200 "snapshot captured" even when the recorder is not enabled (silent data loss on a debug endpoint) — return an explicit not-enabled status; `middleware.go`'s private `statusError` uses `fmt.Errorf` instead of the go-error-family convention.
- [ ] **Battery program waves W3-W5 (triaged 2026-09-04, no work started — long-term).** W3 handler DX: new `httpx` module (B1 ResultHandler family — error mapping MUST share errorpages' taxonomy, pinned by a classification-parity test; B2 bind+validation; B9 no-leak error responses; then B3 ResponseWriter contract, B4 conditional GET — promotes the unused go-etag indirect dep, B5 content negotiation, B7 per-route write deadlines; then B6 route introspection + golden test, B8 route metadata → docs feed). W4 ops & data: `worker` supervisor+pool, `sqlite` ops kit (lease/backup/ledger), `polite` outbound client, `do` bridge, D7 idempotency store, D4 atomic file write (floor: go-atomic-write ≥ v0.5.1, Windows syscall trap), D6 webhook, F1/F4 config modules. W5 realtime completions: C1 drop/backpressure counters (covers the 08-53 `WithOnDrop` audit item — dedupe, do not double-track), C3 per-subscriber auth+filter, C2 projection→broadcast folded contract (the must-have — without it every cqrs+realtime consumer re-runs the pre-fold-state race). Full API sketches, effort sizing, and acceptance tests: `docs/feedback/processed/2026-09-04_batteries-included-sdk-gap-analysis.md` — the canonical spec; do not duplicate its content here.
- [ ] **Integration-module expansion (harvested 2026-09-16 from the 2026-09-04 cqrs-htmx report — the three known seams are still untested).** (1) otel one-`Setup`-per-process contract across go-appkit/otel + go-cqrs-lite/otel globals; (2) `cqrs.OTelProjectionMetrics` end-to-end against the otel module's meter; (3) errorpages family→status parity vs `appkit.HTTPStatus` through a live service. Then the core composition-contract suite (readiness composition, drain transitions, response parity — mirrors cqrs-htmx's `RunWithAppkit` set). Also: add `./integration` to the CI matrix (`.github/workflows/ci.yml` covers 9 modules; dependabot covers integration, CI does not). Source: `docs/status/archived/2026-09-04_17-38_cqrs-htmx-leverage-session-status.md` §b-3/§b-4/§c-5/§f-3.
- [ ] **Health-module quality parity (flightrecorderhealth is the house bar, health is missing all of it):** runnable godoc examples (`example_test.go` with verified output), `NewProbe` batch benchmark (N=1/5/20 checks), fuzz targets (mount options, panicking checks), compile-time contract assertion (`var _ dashboard.Prober = (*health.Probe)(nil)`), aggregate multi-probe example, govulncheck run, decide+test the `WithProbeRoutes`+`WithDashboard` conflict semantics (currently silently ignored), test the documented `WithDashboard`+`WithBasePath` uniform-routing claim. Source: `docs/status/archived/2026-09-04_16-52_go-health-leverage-audit-and-health-module.md` §c-2..§c-6/§c-9/§f-4/§f-5.
- [x] **Error-classification sweep — DONE 2026-09-16.** (1) otel's three sentinels (`errShutdown`, `errBuildRes`, `errStdoutSetup`) are now `errorfamily.NewInfrastructure` (codes `otel.shutdown_incomplete` / `otel.resource_build_failed` / `otel.stdout_exporter_failed`; messages preserved) — consumers get 503 classification; pinned by the new `TestSentinelsClassified` + `errorfamilytest.Assert*` in the shutdown test. (2) cqrs in-flight drain error is now `WrapInfrastructuref` (`cqrs.drain_inflight_failed`). (3) `errorfamilytest.Assert*` adopted in cqrs `staleness_test.go` (3 hand-rolled sites: family + code + retryable + HTTP status) and otel; health and errorpages tests had NO hand-rolled classification assertions (errorpages' family→status table is genuine handler-behavior coverage, deliberately left raw). Suites `-race` green, 0 lint issues in both modules. Source: `docs/status/2026-09-16_08-53_library-utilization-audit-and-fixes.md` §c-7/§c-8.
- [ ] **Flightrecorder ops preset + metrics bridge:** `WithRecorderOptions` passthrough with a documented production preset (`WithSnapshotDir`/`WithMaxSnapshots`/`WithCompression`/`WithMetrics`); wire the fr `MetricsHook` counters into the otel module's meter. Source: 08-53 §c-5.
- [ ] **Dashboard hardening passthrough in health:** `WithDashboard` presets for `WithShutdownDrain`, `WithNonce`+`RecommendedCSP`, `WithRateLimit` (one wiring call should not mean choosing between the dashboard and a hardened posture). Source: 08-53 §c-3.
- [ ] **Upstream ask (verify-before-filing): go-sse dedup-aware `ReplayFiltered`** — `realtime/handler.go` re-implements replay filtering for live-dedup because upstream `ReplayFiltered` has no dedup; propose the variant with the reference implementation attached. Source: 08-53 §c-9.
- [ ] **errorpages: replace the hand-rolled `statusRecorder` with `httputil.ResponseRecorder` (USER GATE: architecture taste).** errorpages' no-direct-httputil minimalism duplicates a wrapper flightrecorder already gets from httputil (transitive dep at v1.1.1 either way) — two ResponseWriter wrappers, one job, one repo. ~10-line change once decided. Source: 08-53 ghost-systems + §g-1.
- [x] **Dependency-currency proof — PROVEN 2026-09-16 against ORIGIN, zero drift.** `git ls-remote --tags` loop over all 14 pinned larsartmann repos (go-appkit self-pin, go-branded-id, go-codec, go-datastar, go-error-family, go-etag, go-flightrecorder, go-health, go-health-dashboard, go-idempotency, go-retry, go-sse, httputil, templ-components), highest-tag-vs-highest-pin across every module go.mod: ALL CURRENT — no module pins behind origin, including today's own train tags (httputil v1.2.0 is the pinned latest in core/otel/flightrecorder/integration). No bumps needed; the "all pins latest" claim is no longer local-clone-only. Source: 08-53 §c-14.
- [x] **Health example live E2E re-run — PASS 2026-09-16, post-two-phase-drain.** Scripted live run of `health/example` (build → serve on :8091): dashboard `GET /health` 200 after the initial sync batch; probe `/readyz` 200 + appkit `/health/ready` 200 pre-drain; dashboard SSE held open with 4 frames captured over 6s (≥2 push intervals; no 30s WriteTimeout cut in that window); on SIGTERM, BOTH surfaces flipped to 503 in lockstep DURING the drain window (`/readyz` 503 + `/health/ready` 503 observed while the process was still serving — the two-phase drain keeps the refresh loop alive); process exited 0 with `drain_hooks` + `shutdown_hooks` + `graceful shutdown complete` in the log. Route map verified live: probe routes at ROOT (`/healthz`, `/readyz`, `/startupz`) when the dashboard is on; appkit ready at `/health/ready`. Source: 08-53 §b-4.
- [ ] **Decide `shutdown phase skipped` log level: keep INFO (explicit visibility) or drop to DEBUG (quieter default)** — grep-able contract, ops taste call; dropped from this list during the 2026-09-16 rebuild and restored the same day. Source: `docs/status/archived/2026-09-04_21-03_batteries-gap-review-triage-wave1-start.md` §f-5.
- [ ] **Decide the single owner for release-state facts** (AGENTS Release State vs TODO_LIST header vs status reports — they agree today only by manual effort; flagged by three separate sessions). Source: `docs/status/2026-09-16_09-38_docs-health-full-repo-audit.md` §f-16.
- [ ] **Root-cause the golines LSP-vs-CLI warning** (LSP flagged `shutdownlog_test.go:240` while the module config + CLI checks were clean; align the LSP's golines settings with the module `.golangci.yml`). Source: 21-03 §b-4.
- [ ] **Verify the "nosurf forks internally" claim in justinas/nosurf's source** (encoded 2026-09-16 into AGENTS/TODO on inference, never read); then add the CSRF pattern-loss known-limitation note to httputil's own README/AGENTS where middleware authors look. Source: `docs/status/2026-09-16_08-40_otel-pattern-propagation-fix-and-self-review.md` §d-4/§b-3.
- [ ] **Version cuts for the pending behavior changes (ride the next train, with the OTEL P2 item):** health (two-phase drain fix — consumer-visible behavior), flightrecorderhealth (hygiene bumps), and add the error-family v0.10.1 bump lines missing from the cqrs/errorpages/root CHANGELOGs (only health + flightrecorderhealth carry them). Source: 08-53 §b-7/§f-7/§f-23.
- [ ] **cqrs + root example doc/test polish:** cqrs README should state `Status()` returns a slice (godoc examples already show it); property/fuzz test for the staleness-budget boundary (exactly-at-threshold); root `example/main.go` predates v0.4.0 — demonstrate `DrainHooks`/`ShutdownHooks`/`OuterMiddlewares` (verified 2026-09-16: the 12-line demo uses none of the headline hooks). Sources: 18-57 §f-21/§f-25/§f-29.

---

_For completed work, see each module's `CHANGELOG.md` and `git log`._
